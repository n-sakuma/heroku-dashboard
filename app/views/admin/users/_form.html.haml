- request_url = @user.new_record? ? admin_users_path : admin_user_path(@user.id)
= form_for @user, url: request_url, html: {method: (@user.new_record? ? :post : :put)} do |f|
  - if @user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@user.errors.count, "error")} prohibited this admin_user from being saved:"
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :email
    = f.text_field :email
  .actions
    = f.submit 'Save'
